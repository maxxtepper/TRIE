#include <memory>
#include "gtest/gtest.h"
#include "src/lib/TermNode.h"

TEST(TermNodeShould, ReturnSomething){
	/*
	std::unique_ptr<TermNode> termnode = std::make_unique<TermNode>();
	std::string actual = termnode->DoSomething();
	std::string expected = Something;
	EXPECT_EQ(expected, actual);
	*/
}
